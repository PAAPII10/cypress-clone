import LoadingScreenComponent from "@/components/global/LoadingScreenComponent";

const loading = () => {
  return (
    <div className="w-full">
      <LoadingScreenComponent />
    </div>
  );
};

export default loading;
